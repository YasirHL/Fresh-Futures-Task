import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fresh-futures-task',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
